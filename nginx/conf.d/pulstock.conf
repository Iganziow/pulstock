# ── HTTP directo (IP o dominio sin SSL) ───────────────────────────────────────
server {
    listen 80 default_server;
    server_name _;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location /static/ {
        alias /var/www/static/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    location /api/auth/token/ {
        limit_req zone=login burst=5 nodelay;
        proxy_pass http://api:8000;
        include /etc/nginx/conf.d/proxy_params.conf;
    }

    location / {
        limit_req zone=api burst=30 nodelay;
        proxy_pass http://api:8000;
        include /etc/nginx/conf.d/proxy_params.conf;
    }
}

# ── HTTPS (descomentar cuando tengas certificados SSL) ───────────────────────
# server {
#     listen 443 ssl;
#     http2 on;
#     server_name api.pulstock.cl;
#
#     ssl_certificate     /etc/letsencrypt/live/api.pulstock.cl/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.pulstock.cl/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#     ssl_session_cache   shared:SSL:10m;
#
#     location /static/ {
#         alias /var/www/static/;
#         expires 1y;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location /api/auth/token/ {
#         limit_req zone=login burst=5 nodelay;
#         proxy_pass http://api:8000;
#         include /etc/nginx/conf.d/proxy_params.conf;
#     }
#
#     location / {
#         limit_req zone=api burst=30 nodelay;
#         proxy_pass http://api:8000;
#         include /etc/nginx/conf.d/proxy_params.conf;
#     }
# }
